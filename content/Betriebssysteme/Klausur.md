Buddy Algorithmus
Anforderungen A,B; Freigabe C
mit den Systemaufrufen  open(), read(), write)(), close()
Device Dateien in /dev
Charachter vs Block device
page-fault, Exception Handler
Map shared vs Map private
I-Node vs FAT + Vorteile
FAT-Diagramm
Datei A, um Datei B ergänzen
Aktive Running blocked beschreiben

void mmap(void * start, size t_length, int prot, int flags, int d, off_t offset)

Map shared vs Map private

Interrupt-Verfahren vs Polling
Inkonsistenz Blöcke

void ueberweisung(int von, nach, betrag)
if(von == nach)
return;

pthread_mutex_lock
b) warum nicht korrekt
c) vorschlag effizienter 
d) %d\n, fork
